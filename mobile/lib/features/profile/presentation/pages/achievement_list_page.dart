import 'package:flutter/material.dart';

class AchievementListPage extends StatelessWidget {
  const AchievementListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('我的成就')),
      body: const Center(child: Text('敬請期待')),
    );
  }
}
